{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt --break-system-packages --user",
  "outputDirectory": ".",
  "installCommand": "pip install -r requirements.txt --break-system-packages --user",
  "framework": null
}
